using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeavyAnt : Ant
{
    public HeavyAnt()
    {
        hitpoints = 25;
        strength = 2;
        defense = 8;
        movement = 2;
        visionRadius = 1;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
